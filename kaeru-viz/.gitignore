node_modules/
dist/
# Generated by scripts/bake-graph-snapshot.sh — contains real (curated) vault
# content; never commit it. The app fetches /graph.json live, or you bake a
# local copy here for an offline demo.
public/graph.json
*.log
